
class Pipe{
        int pipeWidth, pipeHeight, pipeX, pipeDist, pipeHole;


public:
        Pipe(int pWidth, int pHeight, int pX, int pDist, int pHole){

        pWidth = pipeWidth;
        pHeight = pipeHeight;
        pX = pipeX;
        pDist = pipeDist;
        pHole = pipeHole;

}

};

